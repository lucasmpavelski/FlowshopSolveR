#include <cassert>
#include <string>
#include <iostream>
#include <algorithm>

/*#include "pso-tunner/HCFlowshopEval.h"

#ifdef NDEBUG
#undef NDEBUG
#endif

void testCompare() {
    myComparatorMaximizingFitness comp_max;
    myComparatorMinimizingFitness comp_min;
    HCParamsParticle particle1;
    HCParamsParticle particle2;
    particle1.fitness(1);
    particle2.fitness(2);
    assert(comp_max(particle1.fitness(), particle2.fitness()));
    assert(comp_min(particle2.fitness(), particle1.fitness()));
}

void testHCParamsParticleCompare() {
    HCParamsParticle particle1;
    HCParamsParticle particle2;
    particle1.fitness(1);
    particle2.fitness(2);
    assert(particle1.fitness() < particle2.fitness());
}

int main() {
  testCompare();
  testHCParamsParticleCompare();
  std::cout << "All tests passed!";
}
*/
